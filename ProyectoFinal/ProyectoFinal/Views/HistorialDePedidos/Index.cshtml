@model IEnumerable<ProyectoFinal.Models.HistorialDePedidos>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CarritoCompra.id_carritoCompra)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Usuario.codigoUsuario)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CarritoCompra.id_carritoCompra)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Usuario.codigoUsuario)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.id_historialPedido }) |
                @Html.ActionLink("Details", "Details", new { id=item.id_historialPedido }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.id_historialPedido })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
